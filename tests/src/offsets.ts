// import { assert } from 'chai';
import { beforeTest, afterTest } from './utils/hooks.js';
// import { createTestElement } from './utils/createTestElement.js';
// import { getOffset } from '../../src/index.js';

describe('offsets: getOffset()', () => {
  beforeEach(beforeTest);
  afterEach(afterTest);

  it(`should measure the document's offset`, function () {});

  it(`should measure the window's offset`, function () {});

  it(`should measure an element's offset`, function () {});
});
